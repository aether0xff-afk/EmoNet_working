좋아. 그럼 이걸로 방향이 거의 고정됐다.

네가 방금 확정한 내용은 이렇게 받아들이면 된다.

* **클러스터 정의**: 구조 기반
* **rewiring 단위**: 구조 기반 클러스터
* **그 외 항목**: 이전에 정한 내용 그대로 유지

기존 설계 문서에는 아직 클러스터가 “명시적 수학 객체”로 완전히 박혀 있진 않지만, `local cluster homeostasis`, 멀티브랜치 히스토리, trait, `H/z/s` 고정 같은 확장 방향은 이미 들어 있었다. 그래서 지금 한 결정은 기존 문서를 자연스럽게 발전시키는 수정이라고 보면 된다.  

# 확정된 현재 설계

## 전체 파이프라인

[
x \rightarrow E_{ctrl}(x,p) \rightarrow h_t \rightarrow \mathcal{N}(\cdot,p,\mathcal{C}) \rightarrow H_{branch} \rightarrow E_H \rightarrow z \rightarrow R_{tone}(z,p) \rightarrow s \rightarrow G_{prompt} \rightarrow LLM \rightarrow y
]

여기서

* `x`: 입력 텍스트
* `E_ctrl`: 텍스트를 바로 4차원 제어 벡터로 바꾸는 인코더
* `p`: long-term trait vector
* `h_t`: 4차원 제어 벡터
* `\mathcal{N}`: 감정 동역학 신경망
* `\mathcal{C}`: 구조 기반 클러스터 집합
* `H_branch`: 멀티브랜치 감정 히스토리
* `E_H`: 히스토리 인코더
* `z`: 잠재 감정 상태 벡터
* `R_tone`: 스타일 회귀기
* `s`: 고차원 스타일 벡터
* `G_prompt`: 스타일→프롬프트 변환기
* `LLM`: 최종 생성기

기존 문서의 핵심 축인 `H → z → s` 분리는 그대로 유지된다.

---

# 1. 클러스터 정의: 구조 기반으로 고정

클러스터는 adjacency matrix `A`에서 **내부 연결 밀도는 높고 외부 연결 밀도는 낮은 뉴런 묶음**으로 정의한다.

즉,

[
\mathcal{C} = {C_1, C_2, \dots, C_K}
]

이고 각 클러스터 `C_k`는 다음 성질을 가진다.

* 클러스터 내부 연결 수가 많음
* 클러스터 바깥으로 향하는 연결은 상대적으로 적음
* 감정 동역학은 뉴런 하나보다 **국소 회로 단위**로 흔들림

이건 네 모델 철학이랑 잘 맞는다.
감정이 “점 하나의 반응”이라기보다 “작은 회로의 상태 전이”처럼 보이게 해주기 때문이다.

## 실전 정의 방식

초기 구현은 이렇게 가는 게 좋다.

### 초기 클러스터링

* 초기 adjacency `A`에서 community detection 적용
* 예: Louvain, Leiden, spectral clustering 중 하나
* 결과로 초기 클러스터 `C_k` 생성

### 유지 규칙

* 학습 중엔 매 step마다 클러스터를 다시 짜지 않음
* 일정 주기 또는 큰 구조 변화가 있을 때만 재평가
* 즉, **rewiring은 자주**, **reclustering은 드물게**

이렇게 해야 구조가 너무 흔들리지 않는다.

---

# 2. rewiring은 구조 기반 클러스터 단위로 수행

이제 rewiring은 뉴런 단위가 아니라 **클러스터 단위 항상성 스트레스**가 임계치를 넘을 때만 일어난다.

## 클러스터 항상성 스트레스

각 클러스터 `C_k`에 대해

[
HS_k
====

\alpha \cdot \frac{|deg_k - deg_k^*|}{deg_k^*}
+
\beta \cdot |a_k - a_k^*|
+
\gamma \cdot VarAct_k
+
\delta \cdot MemStress_k
]

여기서

* `deg_k`: 클러스터 평균 연결도
* `deg_k^*`: 목표 연결도
* `a_k`: 평균 활성도
* `a_k^*`: 목표 활성도
* `VarAct_k`: 내부 활성 분산
* `MemStress_k`: 기억 잔향 과축적 정도

그리고

[
HS_k > \tau_{rewire}
]

일 때만 해당 클러스터에서 rewiring 발생.

## rewiring 동작

허용되는 동작은 이런 식으로 제한한다.

* 약한 intra-cluster edge 제거
* 필요한 intra-cluster edge 추가
* 과도한 inter-cluster edge 약화
* self-loop 생성/삭제
* 임계값 또는 gain 미세 조정

즉, 완전 랜덤 재배선이 아니라 **항상성 회복용 제한적 구조 수정**이다. 기존 문서의 “제한적 rewiring”, “항상성 스트레스 계산”, “변경 비율 상한”과 이어진다.

## 안정화 규칙

이건 같이 넣는 게 좋다.

* 한 timestep당 변경 가능한 edge 비율 상한
* 같은 클러스터의 rewiring cooldown
* 한 응답 내 총 rewiring budget 제한
* reclustering은 rewiring보다 훨씬 드물게

이걸 안 넣으면 구조가 출렁일 수 있다.

---

# 3. 다른 항목은 그대로 유지

## 제어 벡터 `h_t`

확장 없이 유지.

[
h_t \in \mathbb{R}^4
]

즉 앞단은 크게 키우지 않고, 복잡성은 뒤쪽 `H`, `z`, `s`, `trait`에서 처리한다.

---

## 브랜치 수

확정:

[
B_{max} = 8
]

따라서 히스토리는

[
H_{branch} \in \mathbb{R}^{B \times T \times d_h}, \quad B \le 8
]

로 간다.

그리고 같이 필요한 것:

* branch score
* pruning
* merge
* dominant branch + residual summary

---

## z 차원

고정값이 아니라 ablation.

[
d_z \in {32, 64, 128}
]

즉 실험으로 비교해서 고른다.

비교 기준은 기존 문서의 평가 항목과 잘 맞는다.

* 히스토리 재현성
* 잠재 표현 분리도
* 스타일 벡터 예측 오차
* style consistency score
* 동일 입력에 대한 궤적 다양성

---

## 스타일 벡터 `s`

고차원 유지.

[
s \in \mathbb{R}^{32}
]

그리고 이건 단순 4축이 아니라 **구조화된 32축**으로 간다. 기존 문서의 초기 4차원 제안은 이제 확장판으로 대체되는 셈이다. 

상위 그룹은 그대로:

* affect
* interpersonal stance
* agency/control
* epistemic stance
* discourse style
* expressive dynamics

---

## trait

반드시 도입.

[
p \in \mathbb{R}^{d_p}
]

초기 권장:

[
d_p = 8 \text{ 또는 } 12
]

trait는 최소한 세 군데에 들어간다.

[
h_t = E_{ctrl}(x,p)
]

[
\mathcal{N} = \mathcal{N}(h_t,p,\mathcal{C})
]

[
s = R_{tone}(z,p)
]

즉

* 입력 해석 편향
* 내부 동역학 기본 성향
* 최종 말투 baseline

에 다 영향을 준다.

---

# 4. 지금 기준으로 사실상 확정본에 가까운 명세

## 최종 구조

[
x \rightarrow E_{ctrl}(x,p) \rightarrow h_t \rightarrow \mathcal{N}(h_t,p,\mathcal{C}) \rightarrow H_{branch} \rightarrow E_H \rightarrow z \rightarrow R_{tone}(z,p) \rightarrow s \rightarrow G_{prompt} \rightarrow LLM \rightarrow y
]

## 확정 사항

* 1차 자극 벡터 `u_t`: 제거
* `h_t`: 4차원 유지
* 클러스터: **구조 기반**
* rewiring: **클러스터 단위 항상성 스트레스 기반**
* `B_max`: 8
* `z`: 32/64/128 ablation
* `s`: 32차원 구조화 스타일 벡터
* trait: 도입

---

# 5. 이제 다음으로 바로 정해야 하는 것

이제 남은 건 거의 “세부 명세 박기” 단계다.

## 우선순위 1

**구조 기반 클러스터를 실제로 어떻게 계산할지**

* Louvain
* Leiden
* spectral clustering
  중에서 하나 고르기

## 우선순위 2

**trait 벡터 차원과 축 이름 고정**

* 8차원으로 갈지
* 12차원으로 갈지

## 우선순위 3

**스타일 32축의 논문 근거 정리**
이건 네가 원한 대로, 다음엔 내가 관련 이론 기반으로 축 이름/정의표 형태로 정리해주면 된다.

## 우선순위 4

**branch pruning / merge 수식**
이걸 정해야 `H_branch`가 실제 구현 가능해진다.

## 미정 / 추가 논의 필요

* reclustering 주기를 고정 주기로 할지, 큰 구조 변화 이벤트 기반으로 할지
* trait 차원을 8로 할지 12로 할지
* `z` 최종 선택 기준을 reconstruction 중심으로 할지 style control 중심으로 할지
* 스타일 32축을 완전 독립 회귀로 둘지, 상위 그룹-하위 축의 계층형 회귀로 둘지

원하면 다음 답변에서 내가 이걸 이어서 **“설계 문서 v2.2 확정본”**처럼 한 번에 정리해줄게.
