좋아.
이제까지 합의한 내용을 전부 반영해서, **바로 구현 가능한 수준의 전체 설계 명세**로 한 번에 정리하겠다.

이번 버전은 그냥 아이디어 요약이 아니라, 거의 **시스템 스펙 문서**처럼 쓰겠다.
특히 네가 강조한 것들을 모두 고정 반영한다.

* 1차 자극 벡터 없음
* 텍스트에서 바로 4차원 제어 벡터 `h_t`
* trait 반드시 포함
* 구조 기반 클러스터
* rewiring은 클러스터 단위
* branch는 **뉴런 단위 실제 path**
* branch는 많이 생기지만 prune/merge 후
* **응답 전체 종료 후 대표 branch 1개만 선택**
* 대표 branch는 neuron path 그대로 저장하되 cluster path도 함께 반영
* `z`는 32/64/128 ablation
* `s`는 32차원 고차원 스타일 벡터

---

# 0. 시스템 목표

목표는 다음 파이프라인을 만드는 것이다.

[
x \rightarrow E_{ctrl}(x,p) \rightarrow h_t \rightarrow \mathcal{N}(h_t,p,\mathcal{C}) \rightarrow {H, \mathcal{B}} \rightarrow b^* \rightarrow E_H(H,b^*) \rightarrow z \rightarrow R_{tone}(z,p) \rightarrow s \rightarrow G_{prompt}(s) \rightarrow y
]

여기서

* `x`: 입력 텍스트
* `p`: 장기 성향(trait) 벡터
* `E_ctrl`: 입력과 trait를 받아 4차원 제어 벡터를 내는 인코더
* `h_t`: 감정 동역학을 움직이는 4차원 제어 신호
* `\mathcal{N}`: 감정 동역학 신경망
* `\mathcal{C}`: 구조 기반 클러스터 집합
* `H`: 응답 동안의 전역 시계열 히스토리
* `\mathcal{B}`: 생성된 branch 집합
* `b^*`: 응답 끝에서 선택된 대표 branch
* `E_H`: 히스토리 + 대표 branch를 압축하는 인코더
* `z`: 잠재 감정 요약 벡터
* `R_tone`: 스타일 회귀기
* `s`: 출력 스타일 벡터
* `G_prompt`: 스타일을 생성 제어 신호로 바꾸는 모듈
* `y`: 최종 출력

---

# 1. 전체 철학

이 시스템은 일반적인 감정 분류기가 아니다.
핵심은 다음이다.

## 1.1 감정은 “라벨”이 아니라 “경로”

감정은 한 번에 “기쁨/슬픔”으로 찍히는 게 아니라,
내부 네트워크에서 정보가 퍼지고 억제되고 남고 병합되는 **이동 경로들**의 결과다.

## 1.2 응답마다 순간 상태는 초기화되지만, 기억과 구조는 남는다

즉 단기 반응은 매 응답마다 리셋되지만

* memory
* synapse
* cluster structure
* trait
* baseline threshold

같은 장기 요소는 유지된다.

## 1.3 branch는 “거시적 후보”가 아니라 “실제 path”

브랜치는 몇 개의 추상적 감정 후보가 아니라,
뉴런에서 뉴런으로 데이터가 전달된 **실제 경로 하나**다.

## 1.4 마지막엔 대표 branch 하나만 남긴다

응답 중에는 여러 path가 생기지만,
끝날 때는 prune/merge/final score를 통해 대표 branch `b^*` 하나를 선택한다.

---

# 2. 고정 하이퍼파라미터

초기 구현 기본값은 아래로 고정한다.

## 2.1 코어 크기

* 뉴런 수 `N = 192`
* excitatory 비율 `0.60`
* inhibitory 비율 `0.25`
* modulatory 비율 `0.15`

즉 대략

* E: 115
* I: 48
* M: 29

## 2.2 시계열 관련

* 내부 timestep 최대 `T_max = 24`
* 최대 branch path 길이 `L_max = 6`

## 2.3 latent / style 관련

* trait 차원 `d_p = 8`
* global history feature 차원 `d_h = 64`
* latent 차원 후보 `d_z ∈ {32, 64, 128}`
* style 차원 `d_s = 32`

## 2.4 branch 관리

* root당 최대 유지 path 수 `K_root = 4`
* 전역 최대 유지 path 수 `K_global = 32`

## 2.5 rewiring 관련

* rewiring trigger threshold `tau_rewire = 0.42`
* prune threshold `tau_prune = 0.05`
* correlation threshold `tau_corr = 0.55`
* cluster cooldown `rewire_cooldown = 4`

---

# 3. trait 설계

trait는 반드시 포함한다.

[
p \in \mathbb{R}^8
]

## 3.1 trait 축

다음 8축으로 고정한다.

1. `threat_sensitivity`
2. `social_sensitivity`
3. `recovery_speed`
4. `impulsive_reactivity`
5. `emotional_inertia`
6. `baseline_warmth`
7. `baseline_directness`
8. `baseline_formality`

## 3.2 의미

앞의 5개는 내부 감정 동역학 편향이다.

* 위협을 크게 느끼는가
* 사회적 신호에 민감한가
* 빨리 회복하는가
* 순간 반응이 큰가
* 감정이 오래 남는가

뒤의 3개는 출력 말투 baseline이다.

* 원래 따뜻한 편인가
* 원래 직설적인가
* 원래 격식 있는가

## 3.3 초기화

초기값은 전부 0으로 시작한다.

[
p_0 = \mathbf{0}
]

## 3.4 업데이트

trait는 매 응답마다 급격히 바꾸지 않고, 세션 단위 EMA로만 미세 업데이트한다.

[
p^{new} = (1-\eta)p^{old} + \eta \hat p
]

권장:

* `eta = 0.01`

---

# 4. 입력 인코더 (E_{ctrl})

1차 자극 벡터 `u_t`는 제거했다.
이제 텍스트에서 바로 4차원 제어 벡터를 만든다.

[
h_t = E_{ctrl}(x,p)
]

## 4.1 출력 형식

[
h_t = [Da_t, S_t, NE_t, M_t] \in \mathbb{R}^4
]

### 축 정의

* `Da`: 접근 동기, 몰입, 보상 기대
* `S`: 안정, 억제, 회복
* `NE`: 경계, 각성, 민감한 감시
* `M`: 둔화, 피로, 처리 저하

## 4.2 구현 구조

### 텍스트 인코더

[
e_x = \text{TextEncoder}(x) \in \mathbb{R}^{768}
]

### trait 결합

[
\tilde e = [e_x ; p] \in \mathbb{R}^{776}
]

### MLP head

[
h_t = \tanh(W_3 \sigma(W_2 \sigma(W_1 \tilde e + b_1)+b_2)+b_3)
]

구현 권장:

* `Linear(776, 256)`
* `GELU`
* `Dropout(0.1)`
* `LayerNorm`
* `Linear(256, 64)`
* `GELU`
* `Linear(64, 4)`
* `tanh`

## 4.3 출력 범위

각 축은 `[-1, 1]`

---

# 5. 감정 동역학 신경망 (\mathcal{N})

핵심 네트워크다.

## 5.1 상태 변수

각 뉴런 `i`는 다음 상태를 가진다.

* `v_i^t`: 막전위/현재 potential
* `o_i^t`: spike 또는 binary activation
* `r_i^t`: fast reaction
* `m_i^t`: slow memory
* `a_i^t`: EMA activity
* `theta_i^t`: threshold
* `g_i^t`: gain
* `type_i`: `E`, `I`, `M`

## 5.2 행렬

* adjacency matrix
  [
  A \in {0,1}^{N \times N}
  ]

* weight matrix
  [
  W \in \mathbb{R}^{N \times N}
  ]

* effective weights
  [
  \hat W = A \odot W
  ]

* control projection
  [
  B \in \mathbb{R}^{N \times 4}
  ]

* trait projection
  [
  P \in \mathbb{R}^{N \times 8}
  ]

## 5.3 업데이트 식

### fast reaction

[
r^t = \tanh(W_{hr}h_t + W_{rr}r^{t-1} + W_{mr}m^{t-1} + W_{pr}p + b_r)
]

### slow memory

[
m^t = \tanh(W_{hm}h_t + W_{rm}r^t + W_{mm}m^{t-1} + W_{pm}p + b_m)
]

### neuron drive

[
u_i^t =
\sum_j \hat W_{ji}o_j^{t-1}
+
B_i h_t
+
P_i p
+
\lambda_r r_i^t
+
\lambda_m m_i^t
]

### membrane update

[
v_i^t = \alpha_i v_i^{t-1} + g_i^t u_i^t - \theta_i^t
]

### spike

[
o_i^t = \mathbf{1}(v_i^t > 0)
]

학습 시 surrogate gradient 사용.

### activity EMA

[
a_i^t = \rho a_i^{t-1} + (1-\rho)o_i^t
]

권장:

* `rho = 0.95`

## 5.4 타입별 제약

### excitatory

[
W_{ij} \ge 0
]

### inhibitory

[
W_{ij} \le 0
]

### modulatory

modulatory 뉴런은 직접 전파보다 threshold/gain/decay에 더 큰 영향을 준다.

예:
[
\theta_i^t = \theta_i^{base} + U_\theta z_M^t
]
[
g_i^t = g_i^{base} + U_g z_M^t
]

---

# 6. 응답 단위 리셋 규칙

## 6.1 응답마다 리셋

* `v_i`
* `o_i`
* `r_i`
* 단기 refractory
* 현재 응답 branch pool
* 현재 응답 임시 전파 로그

## 6.2 유지

* `A`
* `W`
* `m`의 장기적 baseline 또는 persistent component
* memory bank
* cluster assignment
* trait `p`
* threshold base
* gain base

즉 short-term state와 long-term structure를 분리한다.

---

# 7. 구조 기반 클러스터 (\mathcal{C})

클러스터는 구조 기반으로 정의한다.

[
\mathcal C = {C_1, C_2, ..., C_K}
]

## 7.1 정의

각 클러스터는

* 내부 연결 밀도 높음
* 외부 연결 적음

인 community다.

## 7.2 초기 생성

### 1단계: 가중 그래프 생성

[
G_w = \frac{|\hat W| + |\hat W|^T}{2}
]

### 2단계: 약한 edge 제거

threshold 이하 edge 제거

### 3단계: Leiden clustering

Leiden 알고리즘으로 community detection 수행

### 4단계: 작은 cluster 병합

size < 8 이면 가장 가까운 큰 cluster로 병합

## 7.3 크기 제약

* 최소 cluster size: 8
* 최대 cluster size: 40
* 총 cluster 수 목표: 6~14

## 7.4 reclustering

reclustering은 자주 하지 않는다.

조건:

* 최근 M응답 동안 전체 edge의 12% 이상 변경
* 또는 modularity 저하
* 최소 20응답 간격

---

# 8. 클러스터 단위 rewiring

rewiring은 뉴런 단위가 아니라 클러스터 단위다.

## 8.1 클러스터 통계

클러스터 `C_k`에 대해

[
deg_k = \frac{1}{|C_k|}\sum_{i \in C_k}\sum_j A_{ij}
]

[
a_k = \frac{1}{|C_k|}\sum_{i \in C_k} a_i
]

[
VarAct_k = \mathrm{Var}({a_i : i \in C_k})
]

[
MemStress_k = \frac{1}{|C_k|}\sum_{i \in C_k} |m_i|
]

## 8.2 항상성 스트레스

[
HS_k
====

\alpha \frac{|deg_k - deg_k^*|}{deg_k^*+\epsilon}
+
\beta |a_k - a_k^*|
+
\gamma VarAct_k
+
\delta MemStress_k
]

권장:

* `deg_k^* = 10`
* `a_k^* = 0.18`
* `alpha = 0.35`
* `beta = 0.30`
* `gamma = 0.15`
* `delta = 0.20`

## 8.3 trigger

[
HS_k > \tau_{rewire}
]

권장:

* `tau_rewire = 0.42`

## 8.4 rewiring 동작

### weak intra-cluster prune

[
|\hat W_{ij}| < \tau_{prune}
]
인 내부 edge 일부 제거

### add coherent intra-cluster edges

activity correlation이 높은 비연결 노드에 edge 추가

[
corr(i,j) > \tau_{corr}
]

### weaken excessive inter-cluster edges

클러스터 바깥으로 향하는 과도한 edge 약화

### threshold/gain micro-adjust

구조 수정으로 충분치 않으면 threshold/gain 조정

## 8.5 rewiring budget

한 번의 rewiring에서 변경 가능한 edge 수:

[
R_k = \min(\lfloor 0.02 \cdot |E_k| \rfloor, 12)
]

## 8.6 cooldown

같은 클러스터는 rewiring 후 4응답 동안 금지
단, `HS_k > 0.8`이면 emergency 허용

---

# 9. branch 정의

여기가 네가 가장 중요하게 고친 부분이다.

## 9.1 branch의 의미

branch는 “감정 후보”가 아니다.
branch는 뉴런 간 실제 정보 전달 경로다.

[
b = (i_0 \to i_1 \to \cdots \to i_\ell), \quad \ell \le L_{max}
]

즉 한 뉴런이 여러 downstream 뉴런으로 데이터를 보내면,
그 갈래 하나하나가 branch다.

## 9.2 저장 방식

branch는 **뉴런 path 그대로 저장**한다.
단, cluster path도 함께 기록한다.

[
c(b) = (C(i_0), C(i_1), ..., C(i_\ell))
]

즉 한 branch는

* raw neuron path
* derived cluster path
  둘 다 가진다.

## 9.3 branch 객체

```python
@dataclass
class BranchPath:
    neuron_path: list[int]
    cluster_path: list[int]
    edge_weights: list[float]
    step_ids: list[int]
    flow_strength: float = 0.0
    activation_support: float = 0.0
    memory_contrib: float = 0.0
    cluster_coherence: float = 0.0
    transition_richness: float = 0.0
    score: float = 0.0
    persistence: int = 0
    alive: bool = True
```

---

# 10. branch 생성 규칙

## 10.1 root 선택

각 timestep `t`에서

[
R^t = { i \mid o_i^t = 1 \land v_i^t > \tau_{root}}
]

권장:

* `tau_root = 0.6`

각 root에서 길이 1 path 시작

[
b = (i)
]

## 10.2 확장

현재 path 끝이 `i_k`일 때 다음 후보는

[
\mathcal N^+(i_k)
=================

{j \mid A_{i_kj}=1,; |\hat W_{i_kj}|>\tau_{edge},; u_j^{t+1}>\tau_{flow}}
]

권장:

* `tau_edge = 0.05`
* `tau_flow = 0.10`

각 후보마다 새로운 분기 생성

[
(i_0 \to \cdots \to i_k \to j)
]

## 10.3 최대 길이

[
L_{max} = 6
]

---

# 11. branch 점수

## 11.1 flow strength

[
F_b = \frac{1}{|\mathcal P_b|-1}\sum_{r=0}^{\ell-1} |\hat W_{i_r i_{r+1}}| \cdot o_{i_r}
]

## 11.2 activation support

[
A_b = \frac{1}{\ell+1}\sum_{r=0}^{\ell} a_{i_r}
]

## 11.3 memory contribution

[
M_b = \frac{1}{\ell+1}\sum_{r=0}^{\ell} |m_{i_r}|
]

## 11.4 cluster coherence

[
C_b = \frac{1}{\ell}\sum_{r=0}^{\ell-1}\mathbf{1}[C(i_r)=C(i_{r+1})]
]

## 11.5 cluster transition richness

[
T_b = \frac{#\text{unique transitions in } c(b)}{\max(1,\ell)}
]

## 11.6 최종 path score

[
s_b =
\lambda_F F_b
+
\lambda_A A_b
+
\lambda_M M_b
+
\lambda_C C_b
+
\lambda_T T_b
-------------

\lambda_L \ell
]

권장:

* `lambda_F = 0.30`
* `lambda_A = 0.22`
* `lambda_M = 0.20`
* `lambda_C = 0.15`
* `lambda_T = 0.08`
* `lambda_L = 0.05`

---

# 12. branch pruning

## 12.1 low-score pruning

[
s_b < \tau_{branch_min}
\Rightarrow remove
]

권장:

* `tau_branch_min = 0.18`

## 12.2 dead-end pruning

다음이면 제거

* 다음 hop 없음
* 최근 2 hop 활성 약함
* memory contribution 작음

## 12.3 per-root top-k

각 root에서 상위 4개만 유지

## 12.4 global top-k

전체 branch 중 상위 32개만 유지

---

# 13. branch merge

## 13.1 merge 기준

### prefix overlap

[
\frac{|prefix(b_1,b_2)|}{\min(|b_1|,|b_2|)} > \tau_{prefix}
]

### node overlap

[
\frac{|V(b_1)\cap V(b_2)|}{|V(b_1)\cup V(b_2)|} > \tau_{node}
]

### cluster path similarity

[
Sim_{cluster}(c(b_1),c(b_2)) > \tau_{cluster}
]

권장:

* `tau_prefix = 0.7`
* `tau_node = 0.6`
* `tau_cluster = 0.75`

## 13.2 merge 결과

새 branch는

* 공통 prefix 유지
* suffix는 더 강한 쪽 사용

score:
[
s_{merge} = \max(s_{b_1}, s_{b_2}) + \eta \min(s_{b_1}, s_{b_2})
]

권장:

* `eta = 0.3`

---

# 14. 대표 branch 선택

대표 branch는 timestep마다 뽑지 않는다.
**응답 전체 끝에서 하나만** 뽑는다.

## 14.1 final candidate set

응답 종료 시 살아남은 branch 집합:

[
\mathcal B_{final}
]

## 14.2 최종 점수

[
Score_{final}(b)
================

\mu_1 s_b
+
\mu_2 M_b
+
\mu_3 P_b
+
\mu_4 E_b^{term}
+
\mu_5 Q_b^{cluster}
]

여기서

* `s_b`: path score
* `M_b`: memory contribution
* `P_b`: persistence
* `E_b^{term}`: 끝노드/끝구간 활성
* `Q_b^{cluster}`: cluster-level quality

권장:

* `mu1 = 0.35`
* `mu2 = 0.20`
* `mu3 = 0.20`
* `mu4 = 0.15`
* `mu5 = 0.10`

대표 branch:

[
b^* = \arg\max_{b \in \mathcal B_{final}} Score_{final}(b)
]

---

# 15. history 저장

이제 `H`는 “branch별 텐서”가 아니라,
응답 동안의 **전역 상태 시계열**이다.

## 15.1 timestep 기록

각 timestep `t`에 대해 아래를 저장한다.

* 전체 평균 활성
* E/I/M 타입별 평균 활성
* 각 cluster 평균 활성
* cluster stress summary
* 현재 branch 수
* pruning된 branch 수
* merge된 branch 수
* top-1 / top-3 score summary
* rewiring event flag
* conflict/dispersion summary
* memory summary

이를 합쳐

[
H_t \in \mathbb{R}^{64}
]

따라서

[
H \in \mathbb{R}^{T \times 64}
]

## 15.2 대표 branch 별도 저장

응답 종료 후 `b^*`를 별도 저장한다.

* `neuron_path(b^*)`
* `cluster_path(b^*)`
* `edge_weights`
* `step_ids`
* `score`
* `memory_contrib`
* `persistence`

---

# 16. history encoder (E_H)

입력은 `H`와 `b^*` 둘 다다.

[
E_H(H, b^*) \rightarrow z
]

## 16.1 global history encoder

`H`를 BiGRU 또는 Transformer로 인코딩

[
z_H = Enc_{global}(H)
]

권장:

* 2-layer BiGRU hidden 128

## 16.2 dominant path encoder

대표 branch를 path encoder로 인코딩

각 hop 토큰:

[
e_r =
[
Emb_{neuron}(i_r);
Emb_{cluster}(C(i_r));
Emb_{pos}(r);
Emb_{edge}(w_{r,r+1})
]
]

이 hop sequence를 GRU/Transformer에 넣는다.

[
z_{path} = Enc_{path}(b^*)
]

권장:

* GRU로 시작
* hidden 128

## 16.3 fusion

[
z_{fusion} = [z_H ; z_{path}]
]

[
z = W_z z_{fusion} + b_z
]

[
z \in \mathbb{R}^{d_z}, \quad d_z \in {32,64,128}
]

---

# 17. latent (z) ablation

고정하지 않고 비교 실험한다.

[
d_z \in {32,64,128}
]

## 17.1 비교 지표

* history reconstruction error
* future prediction error
* style regression error
* latent separability
* trajectory diversity
* style consistency

## 17.2 최종 선택 점수

[
Score(d_z)=
0.30(1-\tilde L_{tone})
+
0.25(1-\tilde L_{hist})
+
0.20\tilde{SC}
+
0.15\tilde{Sep}
+
0.10\tilde{Div}
]

가장 높은 `d_z` 채택

---

# 18. 스타일 벡터 (s)

[
s \in \mathbb{R}^{32}
]

## 18.1 상위 그룹

### affect (6)

1. positive_valence
2. negative_valence
3. arousal
4. calmness
5. dominance
6. submissiveness

### interpersonal stance (6)

7. warmth
8. empathy
9. reassurance
10. hostility
11. social_distance
12. protectiveness

### agency/control (6)

13. assertiveness
14. deference
15. cooperativeness
16. confrontationality
17. guidance
18. permission_seeking

### epistemic stance (6)

19. certainty
20. hedging
21. confidence
22. tentativeness
23. reflectiveness
24. spontaneity

### discourse style (6)

25. verbosity
26. directness
27. formality
28. concreteness
29. abstraction
30. figurativeness

### expressive dynamics (2)

31. humor_playfulness
32. emphasis_intensity

## 18.2 tone regressor

[
s = R_{tone}(z,p)
]

권장 구조:

* `Linear(d_z + 8, 256)`
* `GELU`
* `Dropout(0.1)`
* `Linear(256, 128)`
* `GELU`
* `Linear(128, 32)`
* `tanh`

---

# 19. prompt generator (G_{prompt})

`G_prompt`는 style vector를 LLM 제어 신호로 바꾼다.

## 19.1 rule-based 1차 버전

예:

* `warmth > 0.4` → 따뜻하고 배려 있게
* `directness > 0.5` → 돌려 말하지 말고 분명하게
* `formality > 0.3` → 약간 격식 있게
* `verbosity < -0.3` → 짧고 핵심만
* `certainty > 0.4` → 단정적 비율 증가
* `hedging > 0.4` → 완곡 표현 사용

## 19.2 safety clip

위험한 출력 방지용

[
hostility = \min(hostility, 0.2)
]

[
confrontationality = \min(confrontationality, 0.25)
]

## 19.3 출력 형식

처음에는 자연어 constraint prompt로 생성한다.
나중에는 special control token으로 바꿔도 된다.

---

# 20. 학습 단계

## Stage 0. control pretrain

텍스트→4차원 `h_t`

[
L_h = \mathrm{MSE}(h,\hat h)
]

## Stage 1. dynamics stabilization

동역학 네트워크 안정화

[
L_{dyn} =
\lambda_1 L_{act}
+
\lambda_2 L_{homeo}
+
\lambda_3 L_{sparse}
+
\lambda_4 L_{branch}
]

## Stage 2. history encoder

`H, b^* → z`

손실:

* future prediction
* history reconstruction
* contrastive trajectory loss

[
L_{hist}=L_{future}+L_{recon}+L_{contrast}
]

## Stage 3. tone regressor

`[z,p] → s`

[
L_{tone}=\mathrm{Huber}(s,\hat s)
]

## Stage 4. style consistency

생성된 응답을 다시 style scorer에 넣어 consistency 맞춤

[
L_{sc}=\mathrm{Huber}(s,\hat s_{gen})
]

## Stage 5. light end-to-end finetune

전체를 작은 lr로 미세조정
단, rewiring은 discrete event라 stop-gradient 또는 surrogate 필요

---

# 21. 손실 함수

## 21.1 control loss

[
L_{control} = \mathrm{MSE}(h,\hat h)
]

## 21.2 activity stability

[
L_{act} = |\bar a - a^*|
]

## 21.3 homeostasis

[
L_{homeo} = \frac{1}{K}\sum_k HS_k
]

## 21.4 sparsity

[
L_{sparse} = \frac{1}{N}\sum_i o_i
]

## 21.5 branch regularization

branch가 과도하게 늘어나지 않게

[
L_{branch} = \lambda_B \max(0, |\mathcal B| - B_{target})
]

권장:

* `B_target = 3`

## 21.6 history loss

[
L_{hist}=L_{future}+L_{recon}+L_{contrast}
]

## 21.7 tone loss

[
L_{tone} = \mathrm{Huber}(s,\hat s)
]

## 21.8 style consistency

[
L_{style-consistency} = \mathrm{Huber}(s,\hat s_{gen})
]

## 21.9 total

[
L_{total}
=========

0.15L_{control}
+
0.12L_{act}
+
0.15L_{homeo}
+
0.08L_{sparse}
+
0.10L_{branch}
+
0.18L_{hist}
+
0.12L_{tone}
+
0.10L_{style-consistency}
]

---

# 22. 데이터셋 설계

## 22.1 샘플 구성

각 샘플은

* input text `x`
* optional context
* target/pseudo-target `h`
* rollout history `H`
* representative branch `b^*`
* style target `s`
* output `y`

를 가진다.

## 22.2 수집 순서

### 1단계

텍스트→4축 `h` 약지도 데이터

### 2단계

trajectory 생성 synthetic dialogue 데이터

### 3단계

32축 style annotation 데이터

### 4단계

최종 생성 결과 human eval

---

# 23. 평가 프로토콜

## 23.1 내부 동역학

* 평균 활성 안정성
* 발산률
* rewiring 이후 connectivity 유지율
* homeostasis 목표 편차
* conflict 감소량

## 23.2 latent 품질

* `z` separability
* history future prediction error
* branch diversity
* representative branch plausibility

## 23.3 최종 출력

* naturalness
* emotion consistency
* style control accuracy
* same-input diversity
* randomness penalty

---

# 24. 구현 파일 구조

```text
emonet/
  config.py
  encoders.py
  traits.py
  dynamics.py
  clustering.py
  rewiring.py
  branching.py
  history.py
  history_encoder.py
  tone_regressor.py
  prompt_generator.py
  losses.py
  trainer.py
  infer.py
```

---

# 25. 핵심 클래스 구조

```python
class TraitState:
    def __init__(self, d_p: int = 8): ...
    def update_ema(self, p_hat): ...

class ControlEncoder(nn.Module):
    def forward(self, text_batch, trait_batch): ...
```

```python
class EmotionalDynamicsNet(nn.Module):
    def reset_episode_state(self, batch_size: int): ...
    def step(self, h_t, p, t): ...
    def forward_rollout(self, h_t, p, Tmax=24): ...
```

```python
class StructuralClusterManager:
    def initialize_from_graph(self, A, W): ...
    def compute_cluster_stats(self, state): ...
    def maybe_recluster(self, A, W, history_stats): ...
```

```python
class ClusterRewirer:
    def compute_homeostasis(self, cluster_stats): ...
    def maybe_rewire(self, A, W, cluster_stats): ...
```

```python
class BranchTracker:
    def spawn_roots(self, spikes, potentials, t): ...
    def expand_paths(self, paths, A, W_eff, drives, cluster_ids, t): ...
    def score_paths(self, paths, activity, memory, cluster_ids): ...
    def prune_paths(self, paths): ...
    def merge_paths(self, paths): ...
    def finalize_dominant(self, paths): ...
```

```python
class HistoryBuffer:
    def add_timestep(self, global_features): ...
    def finalize(self): ...
```

```python
class HistoryEncoder(nn.Module):
    def forward(self, H, dominant_branch): ...
```

```python
class ToneRegressor(nn.Module):
    def forward(self, z, p): ...
```

```python
class PromptGenerator:
    def generate_constraints(self, s): ...
```

---

# 26. 추론 절차

입력 한 번에 대해 전체 inference는 이렇게 간다.

## Step 1

trait `p` 읽기

## Step 2

텍스트 `x`를 `E_ctrl`에 넣어 `h_t` 생성

## Step 3

응답 단위 상태 리셋

## Step 4

`T_max` 동안 dynamics rollout 수행

* 뉴런 상태 업데이트
* cluster stats 계산
* 필요 시 rewiring
* branch spawn/expand/score/prune/merge
* global history `H_t` 저장

## Step 5

응답 종료 후 surviving branch 중 대표 `b^*` 선택

## Step 6

`E_H(H,b^*)`로 `z` 생성

## Step 7

`R_tone(z,p)`로 `s` 생성

## Step 8

`G_prompt(s)`로 생성 constraint 생성

## Step 9

최종 응답 `y` 생성

---

# 27. 바로 시작할 때의 최소 구현 순서

가장 현실적인 MVP 순서는 이거다.

## v0

* ControlEncoder
* EmotionalDynamicsNet 기본 업데이트
* 구조 기반 cluster init
* cluster-based rewiring
* BranchTracker
* HistoryBuffer
* HistoryEncoder
* ToneRegressor
* rule-based PromptGenerator

## v1

* trait EMA update
* reclustering
* style scorer
* end-to-end consistency training

---

# 28. 지금 버전의 핵심 요약

이 설계의 본질을 한 문장으로 줄이면 이거다.

> 입력 텍스트와 trait가 4차원 제어 신호를 만들고, 그 신호는 구조 기반 클러스터를 가진 감정 동역학 신경망 안에서 다수의 뉴런 경로(branch)를 생성한다. 이 경로들은 pruning과 merge를 거쳐 응답 종료 시 하나의 대표 branch로 요약되며, 전역 히스토리와 함께 잠재 감정 벡터 `z`로 압축되고, 다시 고차원 스타일 벡터 `s`로 변환되어 최종 출력 말투를 제어한다.

---

# 29. 최종 확정 사항

* `u_t` 제거
* `h_t` 4차원 유지
* trait 포함
* 구조 기반 클러스터
* rewiring은 클러스터 단위
* branch는 뉴런 path
* neuron path 그대로 저장
* cluster path도 함께 반영
* 대표 branch는 응답 끝에서 하나만 선택
* `H`는 전역 시계열
* `E_H(H,b^*) → z`
* `z`는 32/64/128 ablation
* `s`는 32차원

---

# 미정 / 추가 논의 필요

* style 32축의 **정확한 라벨링 프로토콜**을 사람 평가 중심으로 할지, LLM judge + 보정 중심으로 할지
* dominant path encoder를 **GRU로 시작할지 Transformer로 시작할지**
* trait를 완전 고정 user embedding으로 둘지, 세션 적응형으로 둘지
* generated text에서 `s`를 역추정하는 style scorer를 어떤 모델로 둘지
* memory state `m`를 완전 persistent로 둘지, persistent component + episode component로 분리할지

원하면 다음엔 이 설계를 그대로 바탕으로 **`config.py`와 PyTorch 골격 코드**를 바로 짜주겠다.
