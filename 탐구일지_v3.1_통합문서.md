# EmoNet v3.1 탐구 일지 통합 문서

## 1. 탐구 주제

**인공지능은 감정을 라벨이 아니라 neural trace로 표현할 수 있을까?**

## 2. 탐구 동기

기존의 감정 분석은 문장을 하나의 감정 라벨로 분류하는 경우가 많다. 예를 들어 어떤 문장을 보고 "분노", "슬픔", "불안", "기쁨"처럼 하나의 이름을 붙이는 방식이다.

하지만 실제 감정은 그렇게 단순하지 않다. 같은 부정 감정이라도 누군가에게 화가 난 것인지, 스스로를 탓하는 것인지, 상황을 통제할 수 없어서 무력한 것인지에 따라 내부 상태와 행동 방향이 달라진다. 또한 감정은 한순간에 고정되는 것이 아니라, 자극을 받은 뒤 시간에 따라 형성되고 유지되며 약해진다.

EmoNet v3.1은 이러한 문제의식에서 출발한다. 이 버전의 핵심 생각은 감정을 단순한 라벨이 아니라, 입력 자극이 신경망을 통과하면서 만드는 **neural activation trace**로 보자는 것이다. 즉, 감정은 "이 문장은 분노다"라는 이름이 아니라, 자극이 내부 노드들을 어떤 순서와 강도로 활성화했는지에 대한 시간적 흔적이라는 관점이다.

이 탐구에서는 EmoNet v3.1의 설계 문서와 실험 결과를 바탕으로, neural trace가 실제로 감정 상태를 표현할 수 있는지 살펴보았다.

## 3. 탐구 문제

이 탐구의 중심 질문은 다음과 같다.

```text
문장 입력으로 생성된 neural activation trace는
valence, arousal, control state, action tendency 같은
감정 구조를 담고 있을까?
```

이를 더 구체적으로 나누면 다음 질문들이 된다.

1. 감정을 단일 라벨이 아니라 trace로 보는 것이 왜 필요한가?
2. neural trace는 어떤 방식으로 만들어지는가?
3. 뉴런 수를 늘리면 감정 trace의 품질이 좋아지는가?
4. branch collapse를 줄이면 trace가 더 감정답게 형성되는가?
5. trace를 조작하면 감정 방향도 바뀌는가?
6. 현재 결과만으로 trace가 감정 표현이라고 주장할 수 있는가?

## 4. 이론적 배경

### 4.1 기존 방식의 한계

일반적인 감정 분석은 문장을 특정 감정 라벨로 분류한다. 이 방식은 간단하고 평가하기 쉽지만, 감정의 복합성을 충분히 담기 어렵다.

예를 들어 두 문장이 모두 부정적이라고 해도, 하나는 타인에게 향한 분노일 수 있고, 다른 하나는 자기 책임감에서 나온 죄책감일 수 있다. 두 감정은 모두 부정적이지만, 감정의 대상, 통제감, 사회적 방향, 행동 경향이 다르다.

따라서 EmoNet v3.1은 감정을 다음과 같은 여러 축의 조합으로 본다.

| 감정 축 | 의미 |
|---|---|
| `valence` | 긍정 또는 부정 방향 |
| `arousal` | 각성도 또는 감정 강도 |
| `target` | 감정이 향하는 대상 |
| `control_state` | 통제 가능성 또는 무력감 |
| `social_orientation` | 사회적 방향성 |
| `action_tendency` | 접근, 회피, 반격, 억제 같은 행동 경향 |

이 관점에서 감정은 하나의 라벨이 아니라, 여러 축이 시간에 따라 움직이며 형성하는 구조이다.

### 4.2 Trace-as-Emotion 관점

v3.1의 핵심 가설은 다음과 같다.

```text
trace는 감정에 대한 부가 설명이 아니라,
감정 상태 표현 그 자체이다.
```

이전 버전에서는 trace가 응답 생성을 돕는 조건 정보에 가까웠다.

```text
stimulus -> episode trace -> response prompt -> generated response
```

그러나 v3.1에서는 trace를 감정 자체의 표현으로 다룬다.

```text
stimulus -> trace dynamics -> emotion-state representation
```

즉, 좋은 응답이 나오는지만 보는 것이 아니라, trace 공간 자체가 감정과 비슷한 구조를 가지는지 검증하려 한다.

## 5. EmoNet v3.1 구조

EmoNet은 입력 episode를 받아 내부 신경망에서 감정 흐름을 형성하고, 그 흐름을 trace와 branch로 기록한다.

전체 흐름은 다음과 같이 정리할 수 있다.

```text
episode
-> stim_vec
-> node activation
-> signal propagation
-> trace
-> branch
-> dominant branch
-> cluster path
-> semantic report
```

### 5.1 입력부

입력부는 외부 문장이나 episode를 EmoNet 내부에서 계산 가능한 자극 벡터로 바꾼다. 이 벡터를 `stim_vec`라고 한다. 중요한 점은 입력을 바로 감정 라벨로 바꾸지 않고, 감정 흐름을 유발할 수 있는 수치 자극으로 변환한다는 것이다.

### 5.2 신경망 처리부

`stim_vec`는 여러 노드를 통과한다. 각 노드는 자신의 고유 성향, threshold, memory, fatigue, 연결 입력에 따라 활성화되거나 비활성화된다.

한 tick에서 일어나는 과정은 다음과 같다.

```text
stim_vec + memory + 연결 입력 - fatigue/억제
-> K 계산
-> threshold 비교
-> 활성화 또는 비활성
-> 다음 노드로 신호 전달
```

이 과정이 여러 tick 동안 반복되면서 감정 trace가 만들어진다.

### 5.3 Trace

trace는 한순간의 결과값이 아니라, tick별 내부 활성 상태를 저장한 시계열 기록이다.

```text
trace = [t0 상태, t1 상태, t2 상태, ..., tT 상태]
```

따라서 trace는 감정이 한 번에 결정되는 것이 아니라, 시간에 따라 형성되는 과정임을 보여준다.

### 5.4 Branch와 dominant branch

branch는 trace 안에서 특정 방향으로 이어진 활성 경로이다. 여러 branch 중에서 가장 강하고 오래 유지되며 최종 상태를 잘 대표하는 경로를 `dominant branch`라고 한다.

이 구조는 감정을 단순 결과가 아니라 내부 경로로 설명할 수 있게 한다. 예를 들어 "분노"라는 라벨만 붙이는 대신, 어떤 자극이 어떤 노드를 거쳐 어떤 행동 경향으로 이어졌는지 볼 수 있다.

## 6. 실험 1: 뉴런 수를 늘리면 감정 표현이 좋아질까?

### 6.1 실험 목적

첫 번째로 살펴본 질문은 다음과 같다.

```text
뉴런 수를 늘리면 neural activation trace가 더 감정답게 군집화되는가?
```

비교한 설정은 다음과 같다.

```text
n_neurons = 256
n_neurons = 512
n_neurons = 1024
```

### 6.2 실험 결과

| Neurons | Mean dominant branch len | len1 ratio | Mean activation density |
|---:|---:|---:|---:|
| 256 | 10.275 | 0.500 | 0.4484 |
| 512 | 20.050 | 0.500 | 0.4635 |
| 1024 | 24.900 | 0.500 | 0.4630 |

결과를 보면 뉴런 수가 늘어날수록 평균 branch length는 증가했다. 즉, 살아남은 trace는 더 길게 이어지는 경향이 있었다.

그러나 `len1 ratio`는 0.500으로 유지되었다. 이는 dominant branch가 길이 1 이하로 붕괴되는 샘플 비율이 줄지 않았다는 뜻이다.

### 6.3 해석

이 결과는 중요한 결론을 보여준다.

```text
뉴런 수를 늘리는 것만으로는 trace-as-emotion 문제가 해결되지 않는다.
```

뉴런 수 증가는 branch를 길게 만들 수는 있지만, 감정 trace가 제대로 시작되지 못하고 collapse되는 문제를 자동으로 해결하지 못했다. 따라서 단순히 모델을 크게 만드는 것보다, 신호가 어떻게 유지되고 억제되며 안정화되는지가 더 중요하다고 볼 수 있다.

## 7. 실험 2: Branch collapse와 dynamics stabilization

### 7.1 Branch collapse란 무엇인가?

branch collapse는 감정 흐름이 충분히 형성되지 못하고 매우 짧은 branch에서 끝나는 현상이다. 감정 trace가 감정 상태를 표현하려면 어느 정도 지속성과 구조가 있어야 한다. 그런데 branch가 너무 짧으면 내부 감정 흐름을 해석하기 어렵다.

### 7.2 실험 목적

두 번째 실험의 목적은 dynamics parameter를 조정해 branch collapse를 줄이는 것이다.

핵심 질문은 다음과 같다.

```text
감정 trace가 너무 짧게 붕괴되는 현상은
어떤 dynamics 조정으로 완화될 수 있을까?
```

### 7.3 주요 결과

v3.1 final report에서는 다음과 같은 비교가 제시된다.

| Setting | mean branch len | len1 ratio | density |
|---|---:|---:|---:|
| baseline full80 | 19.1875 | 0.4625 | 0.494718 |
| persistent_less_inhibition full80 | 37.9625 | 0.0000 | 0.947035 |
| final_dynamics_v1 full80 | 33.2375 | 0.0875 | 0.839878 |

`persistent_less_inhibition`은 collapse를 거의 제거했지만, activation density가 0.947035로 매우 높았다. 이는 거의 모든 노드가 과도하게 켜지는 상태에 가깝다.

최종 선택된 `final_dynamics_v1`은 `len1 ratio`를 0.0875까지 낮추면서 density를 0.839878로 낮췄다. 완벽하지는 않지만 collapse 방지와 sparsity 사이의 절충점으로 선택되었다.

### 7.4 해석

이 실험은 다음을 보여준다.

```text
capacity alone is insufficient;
dynamics stabilization is required.
```

즉, 뉴런 수만 늘리는 것으로는 부족하고, threshold, inhibition, fatigue, signal persistence 같은 dynamics를 조절해야 감정 trace가 안정적으로 형성된다.

## 8. 실험 3: Neural trace는 감정 구조를 담는가?

### 8.1 실험 목적

세 번째 질문은 neural trace가 실제로 감정 관련 구조를 담고 있는지 확인하는 것이다.

검토한 감정축은 다음과 같다.

- valence
- arousal
- target
- control_state
- social_orientation
- action_tendency_class
- episode_family
- appraisal_family

### 8.2 최종 geometry 결과

v3.1 final report의 `branch_mean` probe 결과는 다음과 같다.

| Axis | NN lift | group separation |
|---|---:|---:|
| valence | +0.1625 | +0.654461 |
| arousal | -0.0750 | +0.542097 |
| target | -0.1000 | -0.138016 |
| control_state | -0.0750 | +0.256330 |
| social_orientation | +0.0125 | +0.082022 |
| action_tendency_class | +0.0125 | +0.391383 |
| episode_family | -0.1250 | +0.383633 |
| appraisal_family | +0.0500 | -0.033200 |

### 8.3 해석

결과를 보면 `valence`는 가장 강하게 분리된다. `arousal`, `control_state`, `action_tendency_class`, `episode_family`도 group separation에서는 어느 정도 양수 구조를 보인다.

하지만 `target`과 blame 방향은 아직 약하다. 이는 neural trace가 모든 감정 의미를 충분히 담는 것은 아니며, 특히 감정이 누구를 향하는지 같은 복잡한 의미는 아직 안정적으로 분리하지 못한다는 뜻이다.

따라서 현재 결론은 다음과 같이 정리할 수 있다.

```text
neural trace는 감정 구조 일부를 담지만,
완전한 감정 표현이라고 주장하기에는 아직 부족하다.
```

## 9. 실험 4: Trace를 조작하면 감정 방향도 바뀔까?

### 9.1 실험 목적

trace가 단순히 감정과 상관관계만 있는 것이 아니라 실제로 감정 방향을 통제하는지 확인하려면 causal test가 필요하다.

핵심 질문은 다음과 같다.

```text
trace axis를 제거하거나 바꾸면
응답의 감정 방향도 예측 가능한 방식으로 바뀌는가?
```

### 9.2 실험 조건

causal proof 설계에서는 각 base record를 다음 조건으로 확장한다.

| Condition | 의미 |
|---|---|
| `trace_full` | 모든 trace field 보존 |
| `ablate_target` | emotion target 제거 |
| `ablate_social_orientation` | social orientation 제거 |
| `ablate_control_state` | control/agency state 제거 |
| `ablate_action_tendency_class` | action tendency 제거 |
| `perturb_target` | target을 대조 값으로 변경 |
| `perturb_social_orientation` | social orientation 변경 |
| `perturb_control_state` | control state 변경 |
| `perturb_action_tendency_class` | action tendency 변경 |

### 9.3 Blind judge 결과

Trace Axis-Only Blind Judge Report에서는 평가자가 친절함, 자연스러움, 상담 품질이 아니라 감정축만 보도록 제한했다.

전체 결과는 다음과 같다.

| Scope | n | success | success rate | tie rate |
|---|---:|---:|---:|---:|
| Overall | 36 | 26 | 0.722222 | 0.444444 |
| Ablation axis original | 12 | 4 | 0.333333 | 0.250000 |
| Perturbation axis shift | 12 | 10 | 0.833333 | 0.083333 |
| Null same response | 12 | 12 | 1.000000 | 1.000000 |

### 9.4 해석

특히 `Perturbation axis shift`의 성공률이 0.833333으로 높았다. 이는 trace를 조작했을 때 감정 방향이 바뀔 가능성을 보여준다.

다만 ablation 조건은 성공률이 낮았다. 즉, trace field를 제거했을 때 원래 감정축이 명확하게 약해지는 효과는 아직 충분히 안정적이지 않았다.

따라서 이 실험은 trace 조작이 감정 방향에 영향을 줄 수 있다는 가능성을 보여주지만, 더 큰 표본과 더 안정적인 평가가 필요하다.

## 10. 전체 결론

EmoNet v3.1의 탐구 결과를 종합하면 다음과 같다.

첫째, 감정을 단일 라벨이 아니라 neural trace로 표현하려는 관점은 타당한 문제의식에서 출발한다. 실제 감정은 단순한 이름보다 복합적인 축과 시간적 변화로 설명하는 것이 더 적절하다.

둘째, neural trace는 감정 구조 일부를 담는 것으로 보인다. 특히 valence는 비교적 잘 분리되며, arousal, control_state, action_tendency 등도 일부 구조를 보인다.

셋째, 뉴런 수를 늘리는 것만으로는 감정 표현이 좋아지지 않았다. branch length는 증가했지만 collapse ratio는 줄지 않았다. 따라서 capacity보다 dynamics stabilization이 중요하다.

넷째, dynamics stabilization은 branch collapse를 줄이는 데 효과가 있었다. 하지만 과활성화와 density 증가라는 부작용도 있어, 최종 설정은 collapse 방지와 sparsity 사이의 절충으로 선택되었다.

다섯째, trace perturbation은 감정 방향을 바꿀 가능성을 보여주었다. 그러나 ablation 효과는 아직 약하며, causal proof를 완성하려면 더 정교한 실험이 필요하다.

최종적으로 다음과 같이 결론 내릴 수 있다.

```text
neural trace는 감정 상태 표현 후보로 가능성이 있다.
그러나 현재 단계에서는 일부 감정 구조를 담는다는 증거가 있을 뿐,
완전한 감정 표현이라고 단정하기에는 추가 검증이 필요하다.
```

## 11. 탐구를 통해 알게 된 점

이번 탐구를 통해 감정을 인공지능이 다루는 방식에 대해 새롭게 생각하게 되었다. 감정을 단순히 "분노", "슬픔", "기쁨"처럼 이름 붙이는 방식은 편리하지만, 실제 감정의 흐름과 복잡성을 충분히 설명하지 못한다.

EmoNet v3.1은 감정을 내부 신경망의 시간적 활성 흐름으로 보려는 시도이다. 이 관점은 감정이 단순 결과가 아니라 과정이라는 점을 잘 보여준다. 특히 trace와 branch를 통해 감정이 어떤 경로로 형성되었는지 관찰하려는 점이 흥미로웠다.

또한 좋은 아이디어가 있다고 해서 바로 성공하는 것은 아니라는 점도 알게 되었다. 뉴런 수를 늘리는 단순한 방법은 branch length를 늘렸지만, collapse 문제를 해결하지 못했다. 결국 중요한 것은 모델의 크기뿐 아니라 내부 dynamics가 어떻게 조절되는지였다.

마지막으로, 연구에서는 성공한 결과뿐 아니라 한계도 중요하다는 것을 느꼈다. v3.1은 trace-as-emotion을 완전히 증명하지 못했지만, 어떤 축은 잘 되고 어떤 축은 약한지 구분했다. 이런 방식이 더 설득력 있는 탐구라고 생각한다.

## 12. 탐구 일지용 최종 요약

```text
탐구 주제:
인공지능은 감정을 라벨이 아니라 neural trace로 표현할 수 있을까?

탐구 동기:
기존 감정 분석은 문장을 하나의 감정 라벨로 분류하는 경우가 많다.
그러나 실제 감정은 시간에 따라 변화하고 여러 요소가 섞여 있다.
EmoNet v3.1은 감정을 neural activation trace로 표현하려는 시도이므로,
이 방식이 감정 상태를 설명할 수 있는지 탐구하고자 했다.

탐구 문제:
문장 입력으로 생성된 neural trace는 valence, arousal, control state,
action tendency 같은 감정 구조를 담고 있을까?

탐구 방법:
EmoNet v3.1의 설계 문서와 실험 보고서를 분석했다.
뉴런 수 변화, branch collapse, activation density, 감정축 분리 결과를 비교했다.
또한 trace를 제거하거나 바꾸었을 때 감정 방향이 바뀌는지 살펴보았다.

탐구 결과:
trace는 valence 등 일부 감정축에서 구조를 보였지만,
target/blame 축은 아직 약했다.
뉴런 수 증가는 branch length를 늘렸으나 collapse 문제를 해결하지 못했다.
dynamics stabilization은 collapse를 줄였지만 과활성화 문제와 균형을 맞춰야 했다.
trace perturbation은 감정 방향을 바꿀 가능성을 보였다.

결론:
neural trace는 감정 상태 표현 후보로 가능성이 있지만,
현재 단계에서 완전한 감정 표현이라고 보기에는 추가 검증이 필요하다.

느낀 점:
감정을 단순 라벨로 분류하는 것보다,
시간에 따른 내부 변화로 보는 관점이 실제 감정에 더 가까울 수 있다고 느꼈다.
또한 인공지능 연구에서는 모델을 크게 만드는 것보다
어떤 구조와 지표로 검증하는지가 중요하다는 점을 알게 되었다.
```

## 13. 참고한 주요 자료

- `v3.1/README.md`
- `v3.1/docs/TRACE_AS_EMOTION_DESIGN.md`
- `v3.1/docs/NEURAL_TRACE_AS_EMOTION_DESIGN.md`
- `v3.1/docs/TRACE_CAUSAL_PROOF_DESIGN.md`
- `v3.1/docs/EXPERIMENT_ROADMAP.md`
- `v3.1/outputs/V3_1_FINAL_REPORT.md`
- `v3.1/outputs/NEURAL_TRACE_CAPACITY_ABLATION_REPORT.md`
- `v3.1/outputs/NEURAL_TRACE_DYNAMICS_STABILIZATION_REPORT.md`
- `v3.1/outputs/TRACE_AXIS_BLIND_JUDGE_REPORT_2026-05-03.md`
- `v3.1/paper/structure_draft/section_5_emonet_structure.md`
