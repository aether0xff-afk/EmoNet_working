# EmoNet 탐구 일지 아이디어/실험 확장안

범위: `v1`부터 `v3.1`까지  
방향: 탐구 일지는 구현 설명보다 **아이디어가 어떻게 생겼고, 어떤 실험으로 확인했는지** 중심으로 구성한다.

## 전체 흐름 요약

```text
v1: 감정을 4차원 벡터와 SNN 흐름으로 표현해보는 초기 아이디어/실험
v2: v1 아이디어를 PyTorch MVP 구조로 모듈화하고, branch/cluster/rewiring/z->s 실험으로 확장
v3: trace와 branch 문제를 본격적으로 실험하고, trace를 감정 표현으로 해석하기 시작
v3.1: trace-as-emotion을 representation/causal proof 대상으로 정리
```

## v1: 이미 충분히 많은 소재

v1은 기존 과제연구 보고서와 연구일지에 이미 많은 내용이 있다. 추가로 더 넣기보다는 기존 내용을 정리하는 쪽이 좋다.

기존 일지에서 확인한 v1 소재:

1. 연구 주제 선정
   - 감정을 깊게 유지하는 챗봇을 만들고 싶다는 문제의식.
   - 신경전달물질 모델과 SNN을 연결하려는 초기 아이디어.

2. 감정 벡터 설계
   - 도파민(D), 세로토닌(S), 노르에피네프린(NE), 멜라토닌(M)의 4차원 벡터.
   - 단순 긍정/부정이 아니라 보상, 안정, 긴장, 피로/회피를 함께 표현.

3. SNN 폭주 문제
   - 신호가 한 tick 안에 모든 뉴런으로 퍼지는 문제.
   - `inbox`와 `next_inbox`를 분리하는 double-buffer 방식으로 해결.

4. 감정 혼합 실험
   - 새 입력 감정과 `current_mood`를 단순 평균으로 섞으면 변화가 밋밋함.
   - decay와 강한 입력의 충격 효과를 넣어 감정이 천천히 회복되게 만듦.

5. 비선형 감정 증폭 실험
   - 입력값이 일정 수준 이상일 때 NE/M이 더 크게 반응하도록 함.
   - 사람이 갑자기 기분이 상하거나 풀리는 느낌을 모사.

6. 시냅스 가소성 실험
   - ΔW가 클 때 연결을 강화하고, 작을 때 연결을 제거.
   - 감정적으로 강한 경험이 이후 반응 경로에 영향을 주도록 함.

7. IPT 기억 실험
   - 이전 입력 문장들이 현재 감정 계산에 영향을 주도록 만듦.
   - 부정적 말이 지나간 뒤에도 NE/M이 쉽게 내려가지 않는 현상을 관찰.

v1은 “아이디어 발생과 초기 실패/수정”이 강하다. 그래서 더 늘리기보다 보고서와 연구일지를 근거로 정리하면 충분하다.

## v2: 추가로 쓸 만한 실험 소재

v2는 README 기준으로 실행 가능한 PyTorch MVP 단계다. 이 시기는 v1의 아이디어를 더 체계적인 모듈로 분해한 단계로 볼 수 있다.

### v2-1. 뉴런 수를 늘리면 감정 표현이 좋아질까?

이미 떠올린 주제. v3.1의 capacity ablation과도 연결된다.

탐구 질문:

```text
뉴런 수가 많아지면 감정 trace가 더 풍부해지고, 감정 표현력이 좋아질까?
```

실험 방식:

- `num_neurons`를 192, 256, 512, 1024처럼 바꿔본다.
- dominant branch length, 활성 밀도, collapse 비율, 처리 시간 비교.
- 너무 작은 모델은 표현력이 부족하고, 너무 큰 모델은 과활성화/잡음이 생기는지 확인.

탐구 포인트:

- 결과가 “크면 무조건 좋다”가 아니어야 탐구성이 생긴다.
- v3.1 결과와 연결하면 “capacity alone is insufficient”라는 결론으로 갈 수 있다.

### v2-2. Branch collapse 문제 해결

이미 떠올린 주제. v2의 `branching.py`와 v3/v3.1 실험을 연결하기 좋다.

탐구 질문:

```text
감정 흐름이 너무 짧은 branch로 끝나는 문제를 어떻게 줄일 수 있을까?
```

실험 방식:

- root 생성 기준인 `tau_root`를 조정한다.
- edge 확장 기준인 `tau_edge`, `tau_flow`를 조정한다.
- branch pruning 기준인 `tau_branch_min`을 조정한다.
- dominant branch length와 len1 ratio를 비교한다.

탐구 포인트:

- 기준이 너무 엄격하면 branch가 안 생긴다.
- 기준이 너무 느슨하면 branch가 너무 많아져 의미가 흐려진다.
- 적절한 균형점을 찾는 것이 핵심이다.

### v2-3. 흥분성/억제성/조절성 뉴런 비율 실험

추가 추천 주제.

탐구 질문:

```text
흥분성, 억제성, 조절성 뉴런의 비율은 감정 안정성과 표현 강도에 어떤 영향을 줄까?
```

실험 방식:

- excitatory 비율을 높인 설정과 inhibitory 비율을 높인 설정을 비교한다.
- 감정 신호가 너무 폭주하는지, 너무 둔해지는지 확인한다.
- 평균 활성도, branch length, style vector의 강도 차이를 본다.

예상 해석:

- 흥분성 뉴런이 많으면 감정 변화가 강하지만 불안정해질 수 있다.
- 억제성 뉴런이 많으면 안정적이지만 감정 표현이 밋밋해질 수 있다.
- 조절성 뉴런은 전체 민감도 조절에 영향을 줄 수 있다.

탐구 일지 제목 예시:

```text
뉴런 종류의 비율은 인공지능 감정 반응의 강도와 안정성을 바꿀까?
```

### v2-4. Rewiring은 감정 경로를 다양하게 만들까?

추가 추천 주제.

탐구 질문:

```text
감정 상태에 따라 연결을 바꾸는 rewiring은 branch 경로를 더 다양하게 만들까?
```

실험 방식:

- rewiring을 켠 경우와 끈 경우를 비교한다.
- cluster별 평균 활동, edge update 수, dominant branch의 반복률을 본다.
- 같은 문장을 여러 번 넣었을 때 경로가 고정되는지, 상황에 따라 바뀌는지 확인한다.

탐구 포인트:

- v1의 ΔW 실험을 v2의 cluster-level rewiring으로 발전시킨 흐름으로 쓸 수 있다.
- “경로가 자꾸 똑같이 흐른다”는 v1 일지의 문제를 v2에서 다시 다루는 구조가 된다.

### v2-5. Cluster 구조는 감정 의미를 정리하는 데 도움이 될까?

추가 추천 주제.

탐구 질문:

```text
뉴런을 cluster로 묶으면 감정 흐름을 더 해석하기 쉬워질까?
```

실험 방식:

- cluster를 쓰는 경우와 쓰지 않는 경우를 비교한다.
- branch를 단순 neuron path로 볼 때와 cluster path로 볼 때 해석 가능성을 비교한다.
- cluster coherence, transition richness, modularity를 지표로 삼는다.

탐구 포인트:

- 단순히 노드 번호를 나열하면 의미가 약하다.
- cluster path로 바꾸면 “위협 감지 -> 평가 -> 행동 경향”처럼 읽을 수 있다.

### v2-6. History encoder는 감정의 시간성을 보존할까?

추가 추천 주제.

탐구 질문:

```text
현재 입력만 볼 때와 이전 history를 함께 볼 때 감정 표현은 어떻게 달라질까?
```

실험 방식:

- history encoder를 사용하는 경우와 dominant path만 사용하는 경우를 비교한다.
- 연속 대화에서 이전 부정 입력이 다음 응답의 style vector에 남는지 본다.
- 단발 입력과 연속 입력의 `z` 차이를 비교한다.

탐구 포인트:

- v1의 `current_mood`, IPT 기억 실험을 v2의 `history -> z` 구조로 발전시킨 주제다.
- 감정의 지속성과 잔상을 탐구하기 좋다.

### v2-7. z의 크기는 감정 정보 보존에 영향을 줄까?

이미 v3 문서에도 있는 좋은 주제.

탐구 질문:

```text
잠재 감정 벡터 z의 차원 수는 감정 정보 보존과 계산 비용 사이에서 어떤 균형점을 가질까?
```

실험 방식:

- `latent_dim = 32, 64, 128` 비교.
- `z -> s` style regression 결과, inference latency, branch summary fidelity 비교.

탐구 포인트:

- 큰 z가 항상 좋은지 확인하는 실험.
- 최종적으로 64처럼 중간 크기를 선택하는 근거를 만들 수 있다.

### v2-8. Style safety clipping은 감정 표현을 너무 순화시킬까?

추가 추천 주제.

탐구 질문:

```text
hostility와 confrontationality를 제한하는 safety clipping은 감정 표현을 지나치게 부드럽게 만들까?
```

실험 방식:

- `safety_hostility_clip`, `safety_confront_clip` 값을 바꿔본다.
- 강한 부정 입력에서 생성되는 prompt constraint가 어떻게 달라지는지 비교한다.
- 감정 보존과 안전성 사이의 균형을 논의한다.

탐구 포인트:

- v3의 softness bias 문제와 연결된다.
- “감정은 강한데 최종 표현은 왜 부드러워지는가?”라는 좋은 탐구 질문으로 확장 가능하다.

## v3: 추가로 쓸 만한 실험 소재

v3는 branch dynamics와 trace 해석이 본격적으로 연구 주제가 되는 단계다.

### v3-1. Trace를 변화시키면 응답도 달라질까?

이미 떠올린 주제.

탐구 질문:

```text
같은 입력 문장이라도 trace 정보를 바꾸면 AI의 감정 해석이나 응답 방향이 달라질까?
```

실험 방식:

- full trace, trace ablation, trace perturbation 비교.
- target, control_state, action_tendency 등을 바꾸어 응답 차이 관찰.

탐구 포인트:

- 단순 상관이 아니라 trace가 응답 방향을 통제하는지 보는 실험이다.

### v3-2. Trace를 감정으로 보자

이미 떠올린 주제. v3.1 핵심 주제로 이어진다.

탐구 질문:

```text
trace는 감정의 설명 자료인가, 아니면 감정 상태 표현 그 자체인가?
```

실험 방식:

- trace space에서 비슷한 감정끼리 가까운지 확인.
- trace를 제거하면 appraisal fidelity가 떨어지는지 확인.
- trace perturbation이 감정 방향을 바꾸는지 확인.

탐구 포인트:

- 탐구 일지의 큰 주제로 가장 좋다.

### v3-3. 뉴런 기능 제거 실험

추가 추천 주제.

탐구 질문:

```text
흥분, 억제, 조절, 기억, rewiring 기능은 각각 감정 동역학에 어떤 기여를 할까?
```

실험 방식:

- full model
- without inhibitory neurons
- without modulatory neurons
- without memory update
- without rewiring
- without branch pruning

비교 지표:

- dominant branch length
- activation stability
- cluster utilization entropy
- response diversity
- emotion appropriateness

탐구 포인트:

- 구조가 복잡한 이유를 보여주는 ablation 실험이다.

### v3-4. Softness bias 문제

추가 추천 주제.

탐구 질문:

```text
내부 trace는 강한 부정 감정을 담고 있는데, 최종 응답은 왜 상담식으로 순화될까?
```

실험 방식:

- style target의 softness, calmness, cooperativeness 분포를 확인한다.
- hostility, resentment, despair 같은 축이 거의 0에 가까운지 본다.
- trace 기반 응답과 style 기반 응답의 차이를 비교한다.

탐구 포인트:

- 감정 상태와 말투 안전성의 충돌을 다룰 수 있다.
- v2의 style safety clipping 실험과 연결된다.

### v3-5. Branch depth는 사고 깊이로 볼 수 있을까?

추가 추천 주제.

탐구 질문:

```text
감정 branch가 더 길다는 것은 더 깊은 감정 처리 또는 사고 과정을 의미할까?
```

실험 방식:

- branch depth가 긴 샘플과 짧은 샘플을 비교한다.
- 응답의 구체성, 감정 적합성, action tendency 반영 정도를 비교한다.
- 너무 긴 branch가 오히려 saturation인지도 확인한다.

탐구 포인트:

- v1 보고서의 “터미널까지 도달하는 과정에서 사용된 뉴런 수를 사고 깊이로 본다”는 아이디어를 발전시킬 수 있다.

### v3-6. 감정 회복 속도 실험

추가 추천 주제.

탐구 질문:

```text
부정적 입력 이후 긍정적 입력이 들어오면 감정 trace는 얼마나 빨리 회복될까?
```

실험 방식:

- 부정 입력 연속 후 긍정 입력을 넣는 시퀀스 구성.
- NE/M 또는 negative valence가 얼마나 빨리 감소하는지 확인.
- memory가 있는 경우와 없는 경우를 비교한다.

탐구 포인트:

- v1의 IPT 기억 실험을 더 정량적인 trace 실험으로 확장할 수 있다.
- 사람의 감정 잔상과 연결하기 좋다.

## v3.1: 심화 탐구 소재

v3.1은 “trace-as-emotion”을 더 과학적으로 증명하려는 단계다.

### v3.1-1. Neural trace geometry 실험

탐구 질문:

```text
neural activation trace는 valence, arousal, action tendency 같은 감정축을 공간적으로 분리할까?
```

실험 방식:

- nearest-neighbor consistency
- group separation
- axis별 NN lift 비교

핵심 결론:

- valence는 비교적 잘 분리된다.
- target/blame 축은 아직 약하다.

### v3.1-2. Capacity alone is insufficient

탐구 질문:

```text
뉴런 수 증가만으로 trace-as-emotion 문제가 해결될까?
```

핵심 결론:

- 뉴런 수 증가는 평균 branch length를 늘린다.
- 하지만 collapse ratio를 자동으로 줄이지 못한다.
- dynamics stabilization이 필요하다.

### v3.1-3. Trace causal proof

탐구 질문:

```text
trace axis를 제거하거나 바꾸면 감정 방향도 예측 가능하게 바뀔까?
```

핵심 포인트:

- ablation보다 perturbation에서 더 강한 효과가 나타났다.
- causal proof는 아직 완성된 증명이 아니라 가능성의 근거다.

## 최종적으로 추가하면 좋은 탐구 일지 목록

v1은 이미 많으므로, 추가 일지는 v2와 v3 중심으로 잡는 것이 좋다.

추천 순서:

1. v2: 뉴런 종류 비율이 감정 안정성에 미치는 영향
2. v2: rewiring이 감정 경로 다양성에 미치는 영향
3. v2: history encoder가 감정 잔상을 보존하는가
4. v2: z 차원 수와 감정 정보 보존의 균형
5. v2: style safety clipping과 감정 순화 문제
6. v3: 뉴런 기능 제거 ablation
7. v3: branch depth는 사고 깊이로 볼 수 있는가
8. v3: 부정 입력 이후 감정 회복 속도
9. v3: trace 변화가 응답 방향을 바꾸는가
10. v3.1: trace를 감정 상태 표현으로 볼 수 있는가

## 가장 좋은 큰 줄기

탐구 일지를 버전별로 흩어 쓰기보다, 다음 큰 줄기로 묶으면 자연스럽다.

```text
1단계: 감정을 4차원 벡터와 SNN 흐름으로 표현해보자.
2단계: 감정 흐름이 폭주하거나 고정되는 문제를 실험으로 해결하자.
3단계: branch, cluster, rewiring, history encoder로 감정 흐름을 구조화하자.
4단계: trace가 단순 기록이 아니라 감정 상태 표현인지 검증하자.
```

이렇게 잡으면 v1은 아이디어와 초기 실패, v2는 구조화 실험, v3/v3.1은 검증 실험으로 역할이 분명해진다.
